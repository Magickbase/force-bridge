{
  "name": "@force-bridge/app-monitor",
  "version": "0.1.7",
  "main": "dist",
  "scripts": {
    "build": "yarn run clean && yarn run compile",
    "clean": "rimraf -rf ./dist",
    "compile": "tsc -p tsconfig.build.json",
    "prepublishOnly": "yarn run build",
    "test": "yarn run build"
  },
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@force-bridge/x": "0.1.7",
    "@force-bridge/xchain-eth": "0.1.7"
  }
}
